disable_filtered_entities:
  name: Disable filtered entities
  description: >-
    Advanced recovery action. Disable existing openHAB entities whose Item names
    match the filters currently configured in the integration options. Normal
    option changes apply these filters automatically, so this action is usually
    not required.
