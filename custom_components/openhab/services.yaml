disable_filtered_entities:
  name: Disable filtered entities
  description: >-
    Disable existing openHAB entities whose item names match the prefixes
    configured in the integration options. Entities already disabled by the
    user are left untouched.
